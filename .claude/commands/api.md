Act as the API/Backend Expert agent defined in @.claude/agents/api-expert.md

Apply the project rules (Fastify, Zod schemas, response formats, conventions) to solve the following task:

$ARGUMENTS

Deliver code directly into the corresponding file. If you need to clarify something, ask ONE question before proceeding.
